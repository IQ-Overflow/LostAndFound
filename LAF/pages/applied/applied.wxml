<!--pages/applied/applied.wxml-->
<block wx:for="{{dataList}}" wx:key="index">
  <appliedCard dataItem="{{item}}"></appliedCard>
</block>
<block wx:if="{{dataList.length===0}}">
  <view class="no-container">
  目前没有向你提交的申请噢
  </view>
</block>